# Bun completions
[ -s "/Users/ebeloded/.bun/_bun" ] && source "/Users/ebeloded/.bun/_bun"

# NVM bash completion
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

# Google Cloud SDK
# if [ -f '/Users/ebeloded/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/ebeloded/google-cloud-sdk/completion.zsh.inc'; fi
